// Plantilla de credenciales — copiar como auth.config.ts y completar con las reales.
// auth.config.ts está en .gitignore y NUNCA debe subirse al repositorio.
export const USUARIOS: Record<string, string> = {
  'Nombre Apellido': 'ContrasenaSegura2026.',
}
